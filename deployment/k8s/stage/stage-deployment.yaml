apiVersion: v1
kind: Namespace
metadata:
   name: stage2
   labels:
     name: stage2
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: stock-stage-api
  namespace: stage2
spec:
  selector:
    matchLabels:
      app: stock
  template:
    metadata:
      labels:
        app: stock
        component: stock-api
    spec:
      containers:
      - name: stock-api
        image: #{DOCKER_REGISTRY}#/stockapi:#{Build.BuildId}#
        ports:
        - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: stock-api
  namespace: stage2
  labels:
    app: stock
    component: stock-api
spec:
  ports:
  - port: 80
    protocol: TCP
  selector:
    component: stock-api